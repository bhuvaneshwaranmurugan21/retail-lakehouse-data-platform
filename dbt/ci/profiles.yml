retail_analytics:
  target: ci
  outputs:
    ci:
      type: redshift
      host: localhost
      port: 5439
      user: ci_user
      password: ci_password
      dbname: analytics
      schema: dbt_ci
      threads: 2
      connect_timeout: 5

